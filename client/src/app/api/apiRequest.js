import axios from 'axios';

export default axios;
// export default axios.create({
//   baseURL: process.env.REACT_APP_PROXY_URL
// });

// Use
/**
 * import appRequest from 'appUrl';
 * const result = await appResult.get(/users);
 */